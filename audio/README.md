Put the licensed store ambient track here as:

happy-nation.mp3

The Ambiente button in `js/vyntra_noir.js` will play that local file. If it is missing, the site falls back to the generated ambient pad so the control still works.
